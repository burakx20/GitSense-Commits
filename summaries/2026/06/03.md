# Activity Summary for 6/3/2026

## 11:42:25 AM
The provided log details changes exclusively within the `c:\Users\burak.bicak\Desktop\MsBuild_Phyton\main.py` file, occurring between 10:42:13 AM and 10:43:12 AM on June 3, 2026.

**File-Specific Updates (`main.py`):**

The `main.py` script implements a `BuildManager` class designed for automating the build, publish, and file extraction processes for .NET projects.

*   **Core Functionality:** The script defines enums for build configuration (`Debug`/`Release`), build type (`Build`/`Publish`), and build results (`SUCCESS`/`FAILED`). The `BuildManager` class initializes with project paths, .NET version, and build modes. It includes methods for:
    *   Determining project, build, and NuGet folder paths.
    *   Executing `dotnet build` or `dotnet publish` commands asynchronously using `asyncio` and `subprocess`.
    *   Copying compiled `.dll`, `.pdb`, and `.nupkg` files from build output directories to specified target folders, while excluding certain files defined in `DoNotCopy`.
    *   Orchestrating the build and file extraction for a predefined list of .NET projects, categorized by their dependencies (e.g., "No Dependency," "Core With Dependency," "Infrastructure With Dependency").
    *   Printing detailed build logs and a summary of all project build results.

*   **Key Changes in Configuration Variables:** The primary modifications across the timestamps are confined to the configuration variables at the end of the script, specifically within the "Değişkenler TKK" (TKK Variables) section:
    *   **Timestamp 6/3/2026, 10:42:51 AM:** The `ProjectRoot` variable was updated from `r"C:\Project\TKK_Framework\framework-tkk"` to `r"C:\Project\TKK Projects Backend\Framework_TKK"`.
    *   **Timestamp 6/3/2026, 10:43:12 AM:** The `TargetFolder` variable was updated from `r"C:\Project\Heroty_Backend\Package\Framework.Package\ExternalDll"` to `r"C:\Project\TKK Projects Backend\Heroty_Backend\Package\Framework.Package\ExternalDll"`.

**Patterns and Recurring Elements:**

*   **Consistent Structure:** The core logic of the `BuildManager` class, including its methods for building, copying, and reporting, remains unchanged throughout the logs.
*   **Project Naming Convention:** The projects managed by the script consistently follow a `Framework.[Area].[SubArea]` naming convention (e.g., `Framework.Core.Extension`, `Framework.Infrastructure.Swagger`).
*   **Asynchronous Operations:** The script consistently uses `asyncio` for executing external `dotnet` commands, indicating a design choice for non-blocking operations.
*   **File Filtering:** The `copy_dll_files`, `copy_pdb_files`, and `copy_nupkg_files` methods consistently filter files based on `startswith('Framework')` and their respective extensions, and also apply an exclusion list (`DoNotCopy`).
*   **Build Modes:** The script is consistently configured for `BuildConfigEnum.Release` and `BuildTypeEnum.Publish` in the "TKK" configuration throughout the observed changes.
*   **Path Management:** Extensive use of `os.path.join` for robust path construction is a recurring pattern.